name: Supabase Activity Scheduler
on:
  schedule:
    - cron: '0 6 * * 1,3'

jobs:
  supabase-activity:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch data from Supabase
        run: |
          curl -s -X GET 'https://vpqqyjfmrqcdjbkgwspw.supabase.co/api/data'
      - name: Verify API response
        run: |
          echo $? (verify API response status code)
